<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="?attr/actionBarSize"
        android:id="@+id/toolbar_hvmain"
        android:background="@drawable/color_backgroud"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:orientation="horizontal"
        android:padding="5dp"
        android:layout_height="wrap_content">
        <AutoCompleteTextView
            android:layout_width="280dp"
            android:layout_height="wrap_content"
            android:hint="Nhập tên hv, tên ph, sđt, địa chỉ,..."
            android:completionThreshold="1"
            android:textSize="15dp"
            android:textColor="@color/black"
            android:id="@+id/edt_search_hv"/>

        <ImageButton
            android:layout_marginLeft="10dp"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/search"
            android:id="@+id/imgbt_search_hv"
            android:onClick="Timkiem"/>

    </LinearLayout>
    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/lv_ad_hocvien"/>
</LinearLayout>